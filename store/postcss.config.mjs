// This store uses plain CSS (no Tailwind). An explicit empty plugin list stops
// Next.js from walking up and inheriting the parent project's PostCSS config.
const config = {
  plugins: {},
};

export default config;
